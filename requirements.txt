# ============================================================
# Biome RAG — Python Dependencies
# Pin major versions; allow minor/patch updates.
# Install: pip install -r requirements.txt
# ============================================================

# --- Web framework ------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.0
pydantic-settings>=2.3.0

# --- HTTP & async -------------------------------------------
httpx>=0.27.0
requests>=2.31.0
websockets>=12.0

# --- Env / config -------------------------------------------
python-dotenv>=1.0.0

# --- Document parsing (Phase 0 legacy + Phase 1 Docling) ----
pypdf>=3.0.0
PyMuPDF>=1.24.0
python-docx>=1.1.0
python-pptx>=0.6.23
beautifulsoup4>=4.12.0
lxml>=5.2.0
# IBM Docling (Phase 1 — layout-aware PDF/DOCX/PPTX/HTML parsing)
docling>=2.0.0

# --- Chunking (Phase 2) -------------------------------------
# Chonkie text chunker (SentenceChunker + SemanticChunker)
chonkie[semantic]>=0.3.0

# --- Embeddings (Phase 3) -----------------------------------
sentence-transformers>=3.0.0
# FlagEmbedding provides bge-m3 dense+sparse in one pass
FlagEmbedding>=1.2.9
torch>=2.2.0
numpy>=1.26.0

# --- Vector databases ---------------------------------------
# Qdrant (Phase 3+)
qdrant-client>=1.10.0
# ChromaDB (Phase 0 legacy — kept for backward compat with existing tests)
chromadb>=0.5.0

# --- BM25 / sparse retrieval --------------------------------
scikit-learn>=1.4.0
rank-bm25>=0.2.2

# --- Reranking (Phase 4) ------------------------------------
# bge-reranker-v2-m3 via FlagEmbedding (already listed above)

# --- LLM providers ------------------------------------------
openai>=1.40.0
anthropic>=0.34.0
groq>=0.9.0

# --- Session memory (Phase 5) --------------------------------
redis>=5.0.0

# --- Observability (Phase 5) ---------------------------------
langfuse>=2.35.0

# --- Orchestration (Phase 1+) --------------------------------
prefect>=3.0.0

# --- Database (Phase 1 PostgreSQL ingestion) -----------------
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# --- Slack ingestion (Phase 1) --------------------------------
slack-sdk>=3.29.0

# --- Evaluation (Phase 6+) ------------------------------------
ragas>=0.1.21
deepeval>=1.0.0

# --- Streamlit dashboard (legacy) ----------------------------
streamlit>=1.35.0

# --- Testing -------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# --- Dev tools -----------------------------------------------
python-multipart>=0.0.9   # FastAPI file uploads
